using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloweenStartSceneScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        //if (this.transform.rotation.y < 50)
        //{
        //    this.transform.rotation.y += 3;
        //}
        //else
        //{
        //    this.transform.rotation.y -= 3;
        //}
       // this.transform.rotation = Quaternion.Slerp(this.transform.rotation, this.transform.rotation + new Vector4(0,50,0,0), 3.0f);

    }
}
